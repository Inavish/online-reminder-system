{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "^/.*\\..+$", "dest": "/$0" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
